"""
Ordering Random Numbers
Create a list called numbers, containing 6 randomly generated numbers and sort it in descending order.
Bonus Challenge: See if you can randomly generate this list in  one line of code.
Bonus Bonus: See if you can create the list in one line in two different ways.
"""
from random import randint,sample
#number = randint(0,100)
#print(number)

numbers = [randint(0,100),randint(0,100),randint(0,100),randint(0,100),randint(0,100),randint(0,100)]
print(f'Unsorted: {numbers}')
print(f'Sorted: {sorted(numbers,reverse=True)}\n')

#Sample(Range(), )
numbers = sample(range(0, 100), 6)
print(f'Unsorted: {numbers}')
numbers.sort(reverse = True)
print(f'Sorted: {numbers}')
